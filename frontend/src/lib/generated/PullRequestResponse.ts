// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * PR data returned in the API response (DB row + rendered body).
 */
export type PullRequestResponse = { body_html: string, id: number, title: string, repo: string, author: string, author_avatar_url: string | null, url: string, ci_status: string | null, last_viewed_at: string | null, body: string, state: string, head_sha: string, additions: number, deletions: number, changed_files: number, draft: boolean, merged_at: string | null, 
/**
 * Raw JSON string, deserialized at the API layer.
 * NULL = team fetch not yet attempted; '[]' = fetched, no matching teams;
 * '[...]' = matched team slugs. Note: migration 009 also documents a
 * 'fetching' sentinel, but no code ever wrote it — the migration comment
 * can't be corrected because sqlx checksums applied migration files.
 * A literal 'fetching' value would fail the JSON parse in to_inbox_item.
 */
teams: string | null, labels: string, };
